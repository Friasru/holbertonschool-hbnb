# HBNB Frontend Testing Checklist

## Prerequisites
1. Backend API running on `http://localhost:5000`
2. Database initialized with sample data
3. Frontend files served (can use `python -m http.server 8000` in part4 directory)

## Backend Setup
```bash
cd part2/hbnb
python run.py